<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tfedummyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TFEDummyApp">
        <activity android:name=".alarmPage"
            android:label="Alarm Details" ></activity>
        <activity
            android:name=".page4"
            android:label="Observation &amp; Actions Report for Work Order" />
        <activity
            android:name=".page3"
            android:label="Work Order" />
        <activity
            android:name=".page2"
            android:label="Device Status of Electrical Devices Group" />
        <activity
            android:name=".MainActivity"
            android:label="Real-time Electrical Measurements">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>